services:
  - type: web
    name: medical-health-checkup
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run src/app.py --server.port $PORT --server.address 0.0.0.0"
